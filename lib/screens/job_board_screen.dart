import 'package:flutter/material.dart';

class JobBoardScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Job Board')),
      body: Center(child: Text('This is the Job Board screen')),
    );
  }
}
